export type AllDataTypes = {
    category: string
    correct_answer: string
    difficulty: string
    incorrect_answers: string[]
    question: string
    type: string
}

export type SelectiveDataTypes = {
    correct_answer: string
    options: string[]
    question: string
}
